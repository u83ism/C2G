# CRADLE 2 GRAVE — 近代化・移行計画

## 方針

- **目標:** レガシーHTMLからSSG（静的サイトジェネレーター）ベースに移行し、コンテンツの保守性・可読性・モバイル対応を向上させる
- **UIデザイン:** 全面刷新。旧来のデザイン（テーブルレイアウト・フリー素材画像）は踏襲しない。画像素材（`gazou/`）も再利用しない
- **制約:** 個人運営・低コスト・シンプルさ優先。オーバーエンジニアリングしない
- **前提:** データ系コンテンツが多く、**コンテンツとデータの扱い方の設計が移行の核心**

---

## SSG候補の評価

| SSG | 言語 | データ連携 | 向き・不向き |
|---|---|---|---|
| **Astro** | JS/TS | JSONファイル・APIを`.astro`内で直接扱える。MDX対応 | **最有力。**文章系・データ系の混在に最も柔軟に対応できる |
| **Hugo** | Go | データファイル（JSON/YAML/TOML/CSV）をテンプレートに流し込める | テンプレートの癖が強い。データ系多めなら候補 |
| **11ty (Eleventy)** | JS | データファイルと組み合わせやすい。シンプル | Astroより設定自由度が高い分、初期設定コストが高い |
| **Next.js** | JS/TS | フル機能。SSG/SSR両対応 | このサイト規模にはオーバースペック |

**推奨: Astro**

理由:
- Markdownファイルをそのまま扱える（既存の`.md`資産を活かせる）
- MDXでReactコンポーネントをMarkdown内に埋め込める（データテーブルの動的表示等に有効）
- コンテンツコレクション機能でフォルダ単位のコンテンツ管理が容易
- GitHub Pagesとの相性が良い（GitHub Actionsで自動デプロイ可能）

---

## データ系コンテンツの扱い方針

レガシーHTMLのテーブルに直書きされているデータをどう扱うかが最大の課題。

### 選択肢

| 方式 | メリット | デメリット | 適用先の目安 |
|---|---|---|---|
| **A. JSONファイルにハードコーディング** | シンプル・外部依存なし | データが多いと管理しにくい | 数十〜百件程度のテーブル |
| **B. CSVファイルで管理** | スプレッドシートで編集可能 | 複雑な構造（ネスト等）が苦手 | フラットな攻略データ |
| **C. ローカルSQLite** | 構造化・クエリ可能 | ビルド時に変換処理が必要 | 非常に大量・複雑なデータ |
| **D. フロント側オブジェクト（JSリテラル）** | ページ内で完結 | SEO・プリレンダリングと相性が悪い | 動的フィルタUI付きページ |

**推奨: A（JSON）+ B（CSV）の組み合わせ**

- フラットなデータ（アイテムリスト等）→ CSV
- 階層・関係性があるデータ（ルート・条件分岐等）→ JSON
- AstroのコンテンツコレクションまたはViteの`import`機能でビルド時に読み込む

### スクレイピングによるデータ抽出

既存HTMLからデータを取り出すための作業：

1. **対象HTMLを特定:** `wildarms/`・`bm/`・`r18/` 等のデータ系ページ
2. **スクレイパーを書く:** Python（`BeautifulSoup`）または Node.js（`cheerio`）で`<table>`要素をパース
3. **出力形式:** CSV or JSONに変換して `src/data/` 以下に配置
4. **確認・補正:** スクレイピング結果を目視確認し、必要に応じて手修正

```python
# スクレイピングのイメージ（Python + BeautifulSoup）
from bs4 import BeautifulSoup
import csv

with open("wildarms/item.htm", encoding="shift_jis") as f:
    soup = BeautifulSoup(f, "html.parser")

rows = []
for tr in soup.select("table tr"):
    cells = [td.get_text(strip=True) for td in tr.find_all("td")]
    if cells:
        rows.append(cells)

with open("src/data/wildarms_items.csv", "w", newline="", encoding="utf-8") as f:
    writer = csv.writer(f)
    writer.writerows(rows)
```

---

## 移行フェーズ

### フェーズ0: 準備（現在）

- [x] リポジトリの現状整理（このドキュメント）
- [ ] Astroプロジェクトの初期セットアップ（`docs/` または新ブランチで試験）
- [ ] GitHub Actions による GitHub Pages 自動デプロイの設定

### フェーズ1: 基盤構築

- [ ] Astroプロジェクト構成の決定（`src/content/`, `src/data/`, `src/pages/` 等）
- [ ] 新デザインの設計（テーマカラー・フォント・レイアウト方針の決定）
- [ ] 共通レイアウト（ヘッダー・フッター・ナビゲーション）の新規作成
- [ ] CSS設計（CSS GridまたはFlexbox。テーブルレイアウト・フリー素材は使わない）
- [ ] トップページの新規作成（`index.htm` の内容を移植しつつデザインは刷新）

### フェーズ2: 文章系コンテンツの移行

優先度が高く、作業コストが低い。

- [ ] `column*.htm` → Markdownに変換して `src/content/column/` に配置
- [ ] `civilization/*.md` → Astroのコンテンツコレクションに取り込み
- [ ] `slay_the_spire/*.md` → 同上
- [ ] `mini/` の文章系ページ（DDR感想等）→ Markdown化

### フェーズ3: データ系コンテンツのスクレイピング・構造化

作業コストが高い。ゲームごとに個別対応。

- [ ] `wildarms/` 全16ページのHTMLをスクレイピングしてデータ抽出
  - アイテム、武器、敵、マップ等を個別JSONまたはCSVに分割
  - Astroページテンプレートでデータをレンダリング
- [ ] `bm/` のデータ抽出・変換
- [ ] `r18/` のデータ抽出・変換
- [ ] `mini/` のデータ系ページ

### フェーズ4: 仕上げ

- [ ] 旧URLからの301リダイレクト設定（URLが変わる場合）
- [ ] 必要な画像のみ新規調達または自作して `public/images/` に配置（旧`gazou/`は使わない）
- [ ] OGP / meta タグの整備
- [ ] サイト全体の動作確認・リンク切れチェック

---

## ディレクトリ構成イメージ（移行後）

```
C2G/
├── src/
│   ├── content/              # Astroコンテンツコレクション
│   │   ├── column/           # コラム（Markdown）
│   │   ├── wildarms/         # ワイルドアームズ（MDX or 自動生成ページ）
│   │   ├── civilization/     # Civilization（Markdown）
│   │   └── slay_the_spire/   # Slay the Spire（Markdown）
│   ├── data/                 # 構造化データ（JSON/CSV）
│   │   ├── wildarms/
│   │   │   ├── items.csv
│   │   │   ├── weapons.json
│   │   │   └── enemies.json
│   │   └── bm/
│   ├── layouts/              # 共通レイアウト
│   ├── components/           # 再利用コンポーネント（DataTable等）
│   └── pages/                # ルーティング
├── public/                   # 静的アセット
│   └── images/               # 旧gazou/は使わない。必要なものだけ新規配置
│       ├── wildarms/
│       └── bm/
├── scripts/                  # スクレイピングスクリプト
│   ├── scrape_wildarms.py
│   └── scrape_bm.py
├── docs/                     # このドキュメント群
│   ├── 現状.md
│   └── 移行計画.md
└── astro.config.mjs
```

---

## 当面の優先アクション

1. **Astroを試す:** `npm create astro@latest` でプロジェクト作成し、感触を確かめる
2. **スクレイパーを1本書く:** `wildarms/` の1ページを対象にデータ抽出を試作し、Astroで表示するところまでやってみる
3. **文章系1ページを移行:** `column.htm` 等の簡単なページでAstroの移行フローを確立する

上記3点で「SSG + データ系コンテンツ」の融合パターンを検証してから、本格移行に入るのが現実的。
